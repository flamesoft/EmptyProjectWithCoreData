EmptyProjectWithCoreData
========================

This is an empty project which uses core data. It is just a reference of core data usage. You can see how the appdelegate files 
are constructed to support core data usage.
